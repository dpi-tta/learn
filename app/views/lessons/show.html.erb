<% content_for :title, @course_lesson || @lesson %>

<div class="container my-2">
  <%= render @lesson %>
  <div class="my-5 d-flex justify-content-center">
    <%= render "course_lessons/nav",  course_lesson: @course_lesson %>
  </div>
</div>

